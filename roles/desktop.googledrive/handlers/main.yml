---
- name: Restart rclone-gdrive mounts
  ansible.builtin.systemd_service:
    name: "rclone-gdrive-{{ item.local_dir | lower }}.service"
    state: restarted
    daemon_reload: yes
  become: yes
  loop: "{{ googledrive_mounts }}"
  loop_control:
    label: "{{ item.local_dir }}"
