- name: Install usbipd
  apt:
    name: usbip
    state: present

- name: Copy usbipd unit to server
  copy:
    src: usbip-attach@.service
    dest:  /etc/systemd/system/usbip-attach@.service

- name: Reload systemd
  systemd:
    daemon_reload: true