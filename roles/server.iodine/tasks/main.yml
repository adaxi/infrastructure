---
- name: Configure iodine defaults
  ansible.builtin.template:
    src: iodine.j2
    dest: /etc/default/iodine
    mode: "0600"
  become: yes

- name: Install iodine
  ansible.builtin.apt:
    name: iodine
    update_cache: yes
  become: yes
